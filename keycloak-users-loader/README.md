# Keycloak users loader

A simple go script to load users from a CSV file into Keycloak.

## Configuration

Modify `config.yaml` as needed.

## Input

See `input/sample.csv` for an example file

## Run

```
go run .
```
